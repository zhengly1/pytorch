name: operator_microbenchmark_pytorch28

on:
  push:
    tags:
      - ciflow/op-benchmark/*
  workflow_dispatch:
  schedule:
    # Run at 06:00 UTC every Sunday
    - cron: 0 6 * * 0

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref_name }}-${{ github.ref_type == 'branch' && github.sha }}-${{ github.event_name == 'workflow_dispatch' }}-${{ github.event_name == 'schedule' }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

jobs:
  opmicrobenchmark-pytorch28-build:
    if: github.repository_owner == 'pytorch'
    name: opmicrobenchmark-pytorch28-build
    uses: ./.github/workflows/_linux-build.yml
    with:
      runner: linux.12xlarge.memory
      build-environment: linux-jammy-cuda12.8-py3.10-gcc9-sm80
      docker-image-name: ci-image:pytorch-linux-jammy-cuda12.8-cudnn9-py3-gcc11
      cuda-arch-list: '8.6 9.0'
      test-matrix: |
        { include: [
          { config: "operator_microbenchmark_pytorch28_test", shard: 1, num_shards: 1, runner: "linux.aws.a100" },
        ]}
      build-additional-packages: "vision audio fbgemm torchao"
    secrets: inherit

  opmicrobenchmark-pytorch28-test:
    name: opmicrobenchmark-pytorch28-test
    uses: ./.github/workflows/_linux-test.yml
    needs: opmicrobenchmark-pytorch28-build
    with:
      timeout-minutes: 500
      build-environment: linux-jammy-cuda12.8-py3.10-gcc9-sm80
      docker-image: ${{ needs.opmicrobenchmark-pytorch28-build.outputs.docker-image }}
      test-matrix: ${{ needs.opmicrobenchmark-pytorch28-build.outputs.test-matrix }}
    secrets: inherit
